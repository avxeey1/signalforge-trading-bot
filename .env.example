# ============================================
# SIGNALFORGE TRADING BOT - ENVIRONMENT VARIABLES
# ============================================

# === TELEGRAM API CREDENTIALS (Required) ===
# Get these from https://my.telegram.org/apps
API_ID=12345678
API_HASH=your_api_hash_here

# === TELEGRAM CHANNEL TO MONITOR (Required) ===
# The channel/group username where signals are posted
# Examples: @signals_channel or https://t.me/signals_channel
TELEGRAM_CHANNEL=@your_channel_username

# === TELEGRAM BOT FOR LOGGING (Optional) ===
# Create a bot via @BotFather and get the token
# The bot will send you logs and notifications
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# === SOLANA WALLET (Required) ===
# Your private key in hex format (64 characters)
# Or export from Phantom as base58 or array format
PRIVATE_KEY=your_private_key_hex_here

# === DESTINATION ADDRESS (Optional) ===
# Default address to send funds to (if needed)
DESTINATION_ADDRESS=optional_destination_wallet_address

# === TRADING PARAMETERS ===
# Amount in SOL to trade per signal (default: 0.0215)
TRADE_AMOUNT_SOL=0.0215

# Target profit multiplier (default: 2.0 means 2x)
TARGET_MULTIPLIER=2.0

# === DASHBOARD SECURITY ===
# Password for web dashboard access (default: admin)
DASHBOARD_PASSWORD=admin123

# Flask secret key - change this in production!
SECRET_KEY=your_secret_key_here_change_this_in_production

# === BOT SETTINGS ===
# Default bot status on startup (running or stopped)
DEFAULT_BOT_STATUS=stopped

# === SOLANA RPC (Optional) ===
# Custom RPC endpoint if needed
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
